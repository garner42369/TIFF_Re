# ==========================================================
# Streamlit Community Cloud 部署专用依赖配置
# 2025年最新优化：移除所有系统依赖，完全依赖 Rasterio 预编译 Wheel
# ==========================================================
#
# ⚠️ 关键优化（2025年4月）：
# 1. 已删除 packages.txt - Rasterio 的 manylinux wheel 已包含完整 GDAL 静态库
# 2. 不再需要 libgdal-dev、gdal-bin 等系统依赖
# 3. 让 pip 自动解析依赖版本，避免版本锁定导致的冲突
#
# 技术原理：
# Rasterio 的 PyPI wheel（manylinux1_x86_64）内部已静态链接：
# - libgdal.so (GDAL 核心库)
# - libgeos.so (几何计算库)
# - libproj.so (投影转换库)
# - 所有其他依赖
#
# 这意味着：pip install rasterio 即可获得完整的 GDAL 功能，
# 无需任何系统级 apt-get 安装！
# ==========================================================

# 核心 Web 框架
streamlit

# 数据处理与矩阵计算
pandas
numpy

# 空间栅格处理引擎
# Rasterio 1.3.9+ 的 manylinux wheel 已包含完整 GDAL 静态库
# 无需任何系统依赖，pip 自动下载并安装
rasterio>=1.3.0
