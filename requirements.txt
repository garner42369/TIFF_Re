# 核心 Web 框架
streamlit>=1.32.0

# 数据处理与矩阵计算
pandas>=2.1.0
numpy>=1.24.0

# ---------------------------------------------------------
# 🚨 终极免编译方案：Rasterio + Pyproj
# ---------------------------------------------------------
# 原生 osgeo.gdal 在云端需要复杂的 C++ 编译环境。
# Rasterio 是由 Mapbox 官方维护的 Python 空间栅格处理库，
# 它不仅完全基于 GDAL 底层构建（等效），而且它的 pip wheel 
# 【自带了预编译好的 GDAL 静态库】。
# 
# 这意味着在 Streamlit Cloud 部署时：
# 1. 0 秒编译！
# 2. 不会再有任何 C++ 或 header 缺失的报错！
# 3. 完全移除外部 pyproj 依赖，全部使用 rasterio 原生空间方法。
# ---------------------------------------------------------
rasterio>=1.3.0
