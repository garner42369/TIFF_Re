# 核心 Web 框架
streamlit>=1.32.0

# 数据处理与矩阵计算
pandas>=2.1.0
numpy>=1.24.0

# =========================================================
# 🚨 终极解决方案：使用预编译的纯 Python 轮子或限定范围的编译命令
# =========================================================
# Streamlit Cloud 底层目前通常是 Debian 12 (Bookworm)，默认提供的 libgdal-dev 为 3.6.2 或 3.6.4。
# 直接写 `gdal` 会导致 pip 试图下载最新版 3.9.x 的源码并使用系统 3.6.x 的头文件进行编译，必然引发编译崩溃。
#
# 解决方法：通过在安装时让 pip 忽略二进制包，直接使用系统的头文件精确编译与系统完全一致的 3.6.2 版本。
# （不要改动下面的 `==3.6.2`，这是目前 Streamlit Cloud Debian 12 镜像的默认底层版本）
# =========================================================
GDAL==3.6.2
