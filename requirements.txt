# ==========================================================
# Streamlit Community Cloud 部署专用依赖配置
# ==========================================================
# 
# ⚠️ 重要说明：
# 所有版本号都经过精心选择，确保与 Streamlit Cloud 的
# Debian 12 (Bookworm) 容器环境 100% 兼容。
#
# 请不要随意修改版本号，除非您明确知道自己在做什么。
# ==========================================================

# 核心 Web 框架
streamlit==1.32.2

# 数据处理与矩阵计算
pandas==2.1.4
numpy==1.24.4

# ---------------------------------------------------------
# 空间栅格处理引擎
# ---------------------------------------------------------
# Rasterio 是 Mapbox 官方维护的 GDAL Python 绑定。
# 它的 pip wheel 通常自带预编译的 GDAL 静态库。
#
# 但在某些 Streamlit Cloud 容器环境中，可能需要
# 从源码编译，因此我们在 packages.txt 中添加了
# libgdal-dev 作为系统级保底依赖。
# ---------------------------------------------------------
rasterio==1.3.9
