# ==========================================================
# Streamlit Community Cloud 容器 (Debian Linux) 专有配置文件
# ==========================================================
# 
# ⚠️ 注意：由于在 app.py 与 requirements.txt 中我们已经弃用了 
# 原生的 osgeo.gdal，转而使用了预编译自带底层库的 Rasterio。
#
# 所以这里可以大幅精简。
# 不再需要 libgdal-dev，也不再需要 build-essential 编译器。
#
# 但是，为了应用某些隐形的 C 语言依赖扩展能正常工作，
# 建议保留以下基础开发包作为系统保底：
python3-dev
python3-numpy
